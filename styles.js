import { StyleSheet } from 'react-native';

export const stylesColumn = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
      flexDirection: 'column'
    },
  });

  export const stylesRow = StyleSheet.create({
    container: {
     flexDirection: 'row'
    },
  });